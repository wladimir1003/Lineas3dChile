export const Install={prompt:null,init(){window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();this.prompt=e});},async show(panel){const ua=navigator.userAgent;const ipad=/iPad/.test(ua)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);const android=/Android/.test(ua);if(android&&this.prompt){this.prompt.prompt();await this.prompt.userChoice;this.prompt=null;return}if(ipad)panel('Instalar en iPad',['Abra esta página en Safari.','Pulse Compartir.','Seleccione Agregar a pantalla de inicio.'],'good');else panel('Instalar',['Abra el menú del navegador y elija Instalar aplicación o Agregar a pantalla principal.']);}};